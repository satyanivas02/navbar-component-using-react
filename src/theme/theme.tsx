import { createTheme } from "@mui/material";

const theme = createTheme({
    typography: {
        h1: {
            fontSize: '20px',
        },
        body1: {
            fontSize: '14px',
        },
    },
})

export default theme;